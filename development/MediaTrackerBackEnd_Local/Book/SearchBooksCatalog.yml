info:
  name: SearchBooksCatalog
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3000/catalog/books/search/Lord of the Rings
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
